import { NgModule } from '@angular/core';

import { MaterialModule as RootMaterialModule } from '../../../shared/material.module';

@NgModule( {
    imports: [
		RootMaterialModule
    ]
} )
export class MaterialModule { }